# Журнал реального тестирования (до 12 ч, 2026-06-18)

Цель: вычистить баги в реальных условиях (живые LLM `192.168.0.31`: gemma-qat:8080 · gemma-e4b:8084 · bge-m3:8083) перед новым этапом. Vault: копия реального `/Users/artem/Documents/Claude/Obsidian-Vault-копия` (231 .md, 85MB). Упор — LLM-фичи (память, эпизоды, RAG, логика, web-агент).

## Окружение
- LLM-серверы: 8080 gemma-qat (chat) · 8084 gemma-e4b (util/fast) · 8083 bge-m3 (embed) — ✅ достижимы.
- Baseline: backend `cargo test` **503+6 passed / 0 failed / 20 ignored**; frontend `vitest` **844 passed**. ✅ зелёный.

## Матрица фаз
| Фаза | Что | Статус |
|---|---|---|
| Ф0 База | полные сьюты + достижимость LLM | ✅ зелёный (503+6 / 844) |
| Ф1 Живой LLM | 19 live-тестов против 192.168.0.31 | ✅ **15 ok / 4 fail** (eval-гейты зелёные; 3 fail = транзиентный 503, 1 = реальный FRESH-баг) |
| Ф2 Аудит | мультиагентный bug-class hunt + adversarial-верификация | ✅ **4 подтверждённых бага** (из 9 сырых) |
| Ф3 Реальный апп | Tauri dev + живой LLM на vault-копии, имитация + стресс | — следующий |
| Ф4 Фикс-цикл | репро→фикс→тест→PR→мерж | ⏳ идёт |

### Ф1 — eval-гейты на РЕАЛЬНЫХ моделях (упор: LLM-фичи)
| Гейт | Результат | Вердикт |
|---|---|---|
| Консолидация памяти MEM-8c | DELETE-precision=**1.000**, UPDATE-quality=**1.000**, predicted_delete=3 | 🟢 PASS |
| Faithfulness эпизодов EP-2 | **1.000** (22/22), 0 галлюцинаций (gemma-qat) | 🟢 PASS |
| Авто-тег AI-2c | precision=0.800, recall=1.000, f1=0.889 | 🟢 PASS (на пороге) |
| RAG/реранк | recall@8=1.000, nDCG=1.000, MRR=1.000 | 🟢 PASS |

→ Рекалибровка не нужна: гейты держатся на текущих gemma-qat/gemma-e4b.

## Журнал багов
| # | Фаза | Баг | Серьёзн. | Причина | Фикс | Статус |
|---|---|---|---|---|---|---|
| B1 | Ф2 | `set_frontmatter_field` затирает внешнюю правку (закрытый буфер) | HIGH | read→atomic_write без сверки диска; внешний писатель (sync/git) в окне теряется; SAFE-3 ловил только открытые буферы | re-read+сверка хеша перед записью → конфликт-ошибка | ✅ фикс |
| B2 | Ф1 | Свежесть web-агента не срабатывает на gemma-e4b | HIGH | парсер ждёт `FRESH:` с двоеточием, модель роняет двоеточие → флаг не ставится + «FRESH» утекает в запрос | парсер принимает `FRESH ` ЗАГЛАВНЫМИ+пробел; +тесты | ✅ фикс |
| B3 | Ф2 | Open Questions виджет: тихий парс-сбой LLM = «нет вопросов» | HIGH | `parse_questions` `unwrap_or_default` → пустой → виджет 'ready', сбой неотличим от пустого | parse → `Result`, парс-сбой → Err (виджет покажет ошибку) + warn-лог; +тест | ✅ фикс |
| B4 | Ф2 | Судья противоречий: парс-сбой кэшируется как «нет противоречия» навсегда | MED | `unwrap_or((false,…))` + cache_put на парс-сбое; ноль логов | парс-сбой → warn + НЕ кэшируем (пере-суд позже) | ✅ фикс |
| B5 | Ф2 | `appendCapture`/inbox read→write без guard внешней правки | MED | окно read→write, внешняя правка теряется (write_file без expected_hash) | отложен (нужен контракт write_file `expectedHash`) — BACKLOG | ⏳ отложен |
| I1 | Ф1 | 3× live-тест chat/news упали на `503 Service Unavailable` :8080 | infra | главная модель перегрузилась/перезагрузилась в момент тестов (eval-гейты на ней же ПРОШЛИ) | перепроверить ре-прогоном; в апп 503→тост-ошибка (не краш) | 🔁 ре-прогон |

## Заметки по ходу
- Ф0: baseline зелёный. Ф1: eval-гейты LLM-памяти/эпизодов/RAG — все 1.000 на реальных моделях. 3 fail = транзиентный 503 (инфра), 1 fail = реальный FRESH-баг (нашёлся только на живой модели). Ф2: 4 подтверждённых бага.
- Фиксы B1-B4 — один backend-PR (503/0 lib-тестов, clippy/fmt чисто). B5 → BACKLOG. I1 → ре-прогон 3 тестов.
