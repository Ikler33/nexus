# Журнал реального тестирования (до 12 ч, 2026-06-18)

Цель: вычистить баги в реальных условиях (живые LLM `192.168.0.31`: gemma-qat:8080 · gemma-e4b:8084 · bge-m3:8083) перед новым этапом. Vault: копия реального `/Users/artem/Documents/Claude/Obsidian-Vault-копия` (231 .md, 85MB). Упор — LLM-фичи (память, эпизоды, RAG, логика, web-агент).

## Окружение
- LLM-серверы: 8080 gemma-qat (chat) · 8084 gemma-e4b (util/fast) · 8083 bge-m3 (embed) — ✅ достижимы.
- Baseline: backend `cargo test` **503+6 passed / 0 failed / 20 ignored**; frontend `vitest` **844 passed**. ✅ зелёный.

## Матрица фаз
| Фаза | Что | Статус |
|---|---|---|
| Ф0 База | полные сьюты + достижимость LLM | ✅ зелёный (503+6 / 844) |
| Ф1 Живой LLM | 19 live-тестов против 192.168.0.31 | ✅ **15 ok / 4 fail** (eval-гейты зелёные; 3 fail = транзиентный 503, 1 = реальный FRESH-баг) |
| Ф2 Аудит | мультиагентный bug-class hunt + adversarial-верификация | ✅ **4 подтверждённых бага** (из 9 сырых) |
| Ф3 Реальный апп | Tauri dev + живой LLM на vault-копии, имитация + стресс | ⏳ идёт (владелец кликает, я читаю лог/код/живой LLM) |
| Ф4 Фикс-цикл | репро→фикс→тест→PR→мерж | ⏳ идёт |

### Ф1 — eval-гейты на РЕАЛЬНЫХ моделях (упор: LLM-фичи)
| Гейт | Результат | Вердикт |
|---|---|---|
| Консолидация памяти MEM-8c | DELETE-precision=**1.000**, UPDATE-quality=**1.000**, predicted_delete=3 | 🟢 PASS |
| Faithfulness эпизодов EP-2 | **1.000** (22/22), 0 галлюцинаций (gemma-qat) | 🟢 PASS |
| Авто-тег AI-2c | precision=0.800, recall=1.000, f1=0.889 | 🟢 PASS (на пороге) |
| RAG/реранк | recall@8=1.000, nDCG=1.000, MRR=1.000 | 🟢 PASS |

→ Рекалибровка не нужна: гейты держатся на текущих gemma-qat/gemma-e4b.

## Журнал багов
| # | Фаза | Баг | Серьёзн. | Причина | Фикс | Статус |
|---|---|---|---|---|---|---|
| B1 | Ф2 | `set_frontmatter_field` затирает внешнюю правку (закрытый буфер) | HIGH | read→atomic_write без сверки диска; внешний писатель (sync/git) в окне теряется; SAFE-3 ловил только открытые буферы | re-read+сверка хеша перед записью → конфликт-ошибка | ✅ фикс |
| B2 | Ф1 | Свежесть web-агента не срабатывает на gemma-e4b | HIGH | парсер ждёт `FRESH:` с двоеточием, модель роняет двоеточие → флаг не ставится + «FRESH» утекает в запрос | парсер принимает `FRESH ` ЗАГЛАВНЫМИ+пробел; +тесты | ✅ фикс |
| B3 | Ф2 | Open Questions виджет: тихий парс-сбой LLM = «нет вопросов» | HIGH | `parse_questions` `unwrap_or_default` → пустой → виджет 'ready', сбой неотличим от пустого | parse → `Result`, парс-сбой → Err (виджет покажет ошибку) + warn-лог; +тест | ✅ фикс |
| B4 | Ф2 | Судья противоречий: парс-сбой кэшируется как «нет противоречия» навсегда | MED | `unwrap_or((false,…))` + cache_put на парс-сбое; ноль логов | парс-сбой → warn + НЕ кэшируем (пере-суд позже) | ✅ фикс |
| B5 | Ф2 | `appendCapture`/inbox read→write без guard внешней правки | MED | окно read→write, внешняя правка теряется (write_file без expected_hash) | отложен (нужен контракт write_file `expectedHash`) — BACKLOG | ⏳ отложен |
| B6 | Ф3 | Новостной LLM-батч теряет все 10 записей при обрезанном/битом ответе — **молча** | MED | `apply_batch` парсил `serde_json` массив ЦЕЛИКОМ → один битый/обрезанный элемент или truncation (нет `]`) ронял весь батч из 10 | **лояльный пер-объектный парс** `parse_eval_objects` (брейс-матчинг с учётом строк/экранирования): обрыв/мусор теряет максимум 1 элемент; +2 теста (обрыв, битый в середине) | ✅ фикс (в NEXUS-be, батч-PR) |
| B7 | Ф3 | Чат тормозит 30–53с на ответ (reasoning крутит вывод 3× по кругу) | HIGH | RAG-чат хардкодил `enable_thinking=true`; gemma-qat переписывает один вывод по кругу; A/B: thinking OFF 5–13с, качество не ниже | тоггл «Быстрый/Глубокий» (per-call `deep`, дефолт Быстрый→`chat_fast`); +лог рассуждений (счётчики INFO, полный текст по `NEXUS_TRACE_REASONING`); тесты+adversarial-ревью | ✅ фикс (в NEXUS-be, батч-PR) |
| B8 | Ф3 | Авто-чип «Запомнить?» всплывает на уже сохранённый факт (замечание владельца) | MED | `memory_propose` не сверял кандидата с существующими фактами (дедуп был только на записи `add`) | `filter_known_exact` отсеивает точные повторы в propose (близкие — зона консолидации); +backend-тест | ✅ фикс (#321) |
| B9 | Ф3 | «Из прошлых разговоров» инжектит НЕРЕЛЕВАНТНЫЕ диалоги (на «новости OpenAI» лезли тестовые про Аристотеля/имя) | MED | `search_memory` (N4b) брал top-k БЕЗ порога релевантности (в отличие от эпизодов 0.45 / фактов 0.30); калибровка bge-m3: нерелев «пол» 0.39–0.44, релев 0.64 | `CHAT_MEM_SIM_THRESHOLD=0.45` фильтр в search_memory; +тест q/−q; eval-гейта на N4b нет → без риска | ✅ фикс (в NEXUS-be, батч-PR) |
| I1 | Ф1 | 3× live-тест chat/news упали на `503 Service Unavailable` :8080 | infra | главная модель перегрузилась в момент первого батча (eval-гейты на ней же ПРОШЛИ) | **ре-прогон: все 3 ПРОШЛИ (77с)** — подтверждённо транзиент, НЕ баг | ✅ закрыт |
| I2 | Ф3 | `news llm_failed=10/30/20` всплеск | infra | контеншн: 3 инстанса (2 моих дубля NEXUS-be + 1 владельца) долбили одну gemma-e4b; батч 30.9с → обрыв стрима | устранены дубли → 1 инстанс; ждём чистый прогон новостей для подтверждения | ⏳ наблюдаю |
| B10 | аудит | **Безопасность:** пин-симлинк `*.md → .nexus` утекает секреты/историю чатов в LLM-канал | **MAJOR** | `is_pinnable` лексический, `resolve_vault_path` ловит побег наружу root, но НЕ служебный `.nexus` (внутри root) | `is_ignored(&abs)` на канонизир. пути (паритет с `attachments.rs`); + бюджет пинов по успешно добавленным | ✅ фикс (audit-PR) |
| B11 | аудит | **Потеря данных:** UPDATE-merge консолидации с текстом=другой факт → UNIQUE-конфликт валит транзакцию → кандидат потерян | **MAJOR** | голый `UPDATE SET text` без проверки коллизии (Supersede-ветка её обрабатывает, UPDATE — нет) | проверка коллизии под tx → деградация в ADD; +тест | ✅ фикс (audit-PR) |
| B12 | аудит | Длинная сессия (>40 реплик) суммируется по ПЕРВЫМ 40, но ватермаркится как полная → не пересжимается | **MAJOR** | окно транскрипта = первые 40, а `last_msg_id`=max сессии → idempotency-гейт блокирует навсегда | окно → ХВОСТ-40 (Variant B: важнее «к чему пришли», ватермарк согласован, сходится) | ✅ фикс (audit-PR) |
| B13 | аудит | `parse_judgment` (противоречия) ломается о прозу со скобками → пара пере-судится каждый прогон | MED | `find('{')`+`rfind('}')` (класс B6) | `first_json_object` — сбалансированный пер-объектный парс; +тест | ✅ фикс (audit-PR) |
| B14 | аудит | Откат UPDATE консолидации падает на UNIQUE-коллизии при restore старого текста | MED | голый restore-`UPDATE` без проверки | проверка коллизии → пропуск элемента (undo и так допускает пропуски) | ✅ фикс (audit-PR) |
| Q1 | eval | Авто-тег флейк-вокруг-порога 0.727↔0.800 (gemma-e4b даёт 2–3 лишних тега из 11) | MED-качество | промпт не давил на точность (recall=1.0 — запас) | усилил промпт «ставь тег только если ЯВНО об этой теме»; 4 прогона → 1.0/1.0/1.0/0.889 (fp 0–1) | ✅ улучшено (audit-PR) |

### Ф3 — реальный апп на vault-копии (живой LLM), результаты по фичам
Среда: инстанс владельца `~/Documents/NEXUS` (main `73816b4` #320, бинарь собран 14:30), vault-копия 231 заметка проиндексирована (0 ошибок), модели `:8080/:8083/:8084` живы. Разделение труда: владелец кликает в GUI (его окно на отдельном macOS Space — computer-use не дотягивается), я читаю бэкенд-лог в реальном времени + код + бью живой LLM.

| Фича | Проба | Вердикт |
|---|---|---|
| Чат + RAG | «Что у меня есть про Аристотеля?» | 🟢 **PASS** — ответ строго по заметкам, цитаты-источники [1][2][3] (заметка «Аристотель», силлогистика, законы мышления, история логики) |
| Память (захват) | авто-предложение после ответа | 🟢 предложение «Добавить в память: ‹…заметка Аристотель›» + «Запомнить» появилось |
| Action-row (P6-AR) | под ответом ИИ | 🟢 Копировать/Вставить в заметку/В память/Перегенерировать видны |
| Новости (LLM-этап) | фоновые прогоны | 🟡 `llm_failed=10/30/20` — контеншн от дублей (I2) + латентная хрупкость B6 |

Каркас приложения (онбординг 4 шага, AI-check «Готов», индексация 231/231) — 🟢.

## Заметки по ходу
- Ф0: baseline зелёный. Ф1: eval-гейты LLM-памяти/эпизодов/RAG — все 1.000 на реальных моделях. 3 fail = транзиентный 503 (инфра), 1 fail = реальный FRESH-баг (нашёлся только на живой модели). Ф2: 4 подтверждённых бага.
- Фиксы B1-B4 — один backend-PR (503/0 lib-тестов, clippy/fmt чисто). B5 → BACKLOG. I1 → ре-прогон 3 тестов.
