# Eval-харнесс качества RAG (`src-tauri/src/eval` + `eval/`)

> Срез Ф1-10 (§6.6, **AC-EVAL-1..6**). По образцу `sa-eval`: golden `вопрос→файлы`, метрики
> **recall@8 / nDCG@8 / MRR**, регресс-гейт против зафиксированного baseline. «Correctness over speed»
> неисполним без измерения — этот харнесс и есть измерение; он же разблокирует калибровку порогов/реранкер.

## Данные
- **`eval/golden.json`** (AC-EVAL-1): корпус разнотемных заметок (RU/EN) + кейсы `query → relevant[]`,
  включая **кросс-язычные** (AC-EVAL-6: RU-запрос по EN-заметке и наоборот). Версионируется, расширяется.
- **`eval/baseline.json`** (AC-EVAL-2): пороги `recall_at_k/ndcg_at_k/mrr` + **условия** (модель, сервер,
  k, набор — AC-EVAL-4). Зафиксированы по фактическому прогону Фазы 1; обновлять осознанно при улучшении.

## Код (`eval/mod.rs`)
- Метрики (бинарная релевантность на уровне ФАЙЛОВ; выдача чанков схлопывается в файлы):
  `recall_at_k`, `reciprocal_rank`, `ndcg_at_k` — чистые, юнит-тестируемы.
- `run_eval(reader, vectors, embedder, cases, k) -> EvalReport`: на каждый кейс — `hybrid_search` →
  уникальные пути → метрики; агрегат = среднее. `EvalReport`/`CaseResult` (Serialize, для отчёта).
- `index_corpus(root, docs, embedder, vectors)`: строит temp-vault из корпуса и индексирует (RAG).
- `load_golden` / `load_baseline` (`include_str!`).

## Прогон (AC-EVAL-3 гейт)
```
# нужен embedding-сервер :8081 (условия из baseline.json)
cargo test eval::tests::live_eval_meets_baseline -- --ignored --nocapture
```
Печатает отчёт (условия + recall@8/nDCG@8/MRR + по-кейсный разбор с hits) и **падает при метриках ниже
baseline** (регресс-гейт). Любое изменение чанкера/RRF-весов/порогов/модели → перепрогнать (AC-EVAL-3).

## Фактический baseline (Фаза 1 → Ф1-12, bge-m3 @ :8083, k=8, 10 кейсов)
**recall@8 = 1.000, nDCG@8 = 0.883, MRR = 0.848.** Все 10 кейсов в recall@8, включая **оба кросс-язычных**
(EN→RU `budget` ранг 7, RU→EN `photosynthesis` ранг 3) → **AC-EVAL-6 закрыт**. Кросс-язычные ранжируются
ниже монолингвальных (отсюда nDCG/MRR < 1) — точка роста (reranker под этим гейтом).
> Для справки: исходный **nomic** @ :8081 давал recall@8 = 0.800 с полным провалом тех же 2 кросс-язычных
> кейсов — eval **количественно подтвердил риск ADR-005**, а смена на bge-m3 (§6.5, переэмбеддизация) его сняла.
Калибровка решений «RRF / порог suggest / 8 чанков» — на этом наборе, не с потолка.

## Тесты
- Юнит (CI): математика метрик (recall/MRR/nDCG), парс golden+baseline, сквозная проводка `run_eval` на
  mock (точное слово → файл в top-k → recall=1). **Живой** (`#[ignore]`): прогон nomic ≥ baseline.
- **Офлайн eval-ГЕЙТ логики ранжирования (V4.5, AC-EVAL-3):** `offline_eval_gate_on_fixed_vectors` —
  детерминированный `cargo test` БЕЗ сервера. `FixedEmbedder` отдаёт фиксированные синтетические векторы
  (cosine-оси apple/banana/cherry); запросы находят релевантные по ВЕКТОРНОЙ близости (токены запроса не
  встречаются в телах → FTS пуст) → реальный `hybrid_search`→RRF→`run_eval` с точно посчитанными вручную
  метриками (recall@8=1.0, MRR=5/6, nDCG≈0.877; кейс `QRYMIX`: cherry@1 cos0.8 > apple@2 cos0.6 → RR=0.5).
  Ловит регрессии слияния RRF и метрик в CI. Гейт на РЕАЛЬНОМ качестве (live baseline) остаётся `#[ignore]`.

## Дальше (BACKLOG)
- **Гейт реального качества в CI без сервера**: засеять golden РЕАЛЬНЫМИ эмбеддингами bge-m3 в файл-фикстуру
  (нужен :8083 один раз) → детерминированный recall/nDCG/MRR на настоящих векторах в CI. Сейчас офлайн-гейт
  (V4.5) проверяет ЛОГИКУ ранжирования на синтетике; реальное качество — живой `#[ignore]`-прогон.
- Поднять кросс-язычный ранг (nDCG/MRR < 1): reranker под этим гейтом; расширение golden по реальному vault.
- Отдельный eval атрибуции источников (§6.6).
- ✅ Мультиязычный эмбеддер (bge-m3) — **сделано (Ф1-12), AC-EVAL-6 закрыт.**
