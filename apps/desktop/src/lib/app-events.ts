/**
 * Имена кросс-модульных window-событий приложения. Паттерн — существующие window-события фронта
 * (подписка на `resize` в `stores/theme.ts` и т.п.), НЕ отдельная шина: эмитент диспатчит на
 * `window`, заинтересованная сторона подписывается сама. Развязывает модули без прямых импортов
 * (F-1: stores не импортируют components).
 */

/** Смена vault завершена (`stores/vault.openVault`): кэши, ключованные путями заметок, недействительны. */
export const VAULT_SWITCHED_EVENT = 'vault:switched';
