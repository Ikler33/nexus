-- NF-6 хвост (фидбэк владельца): у HN-айтема `url` = отправленная ссылка (для Show HN — github-репо,
-- корректный «оригинал»), но ссылка на САМО ОБСУЖДЕНИЕ на HN терялась. Храним её отдельно, чтобы
-- ридер показал кнопку «Обсуждение на HN» рядом с «Оригинал». Nullable: не-HN и текстовые HN-посты
-- (там url уже = обсуждение) → NULL; существующие строки бэкфилл NULL.
ALTER TABLE news_items ADD COLUMN comments_url TEXT;
